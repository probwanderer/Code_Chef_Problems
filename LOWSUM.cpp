#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
		int k;
		int q;
		cin>>k>>q;
		

		ll *m=new ll[k];
		ll *s=new ll[k];
		
		ll *sum=new ll[k*k];
		for(int i=0;i<k;i++)
			cin>>m[i];
		sort(m,m+k);
		for(int i=0;i<k;i++)
		{
			cin>>x;
			if()
		}
		
		sort(s,s+k);
		for(int i=0;i<n;i++)
		{

		}
		while(q--)
		{
			;ll x=0;
		x=m[p-1]+s[p-1];
			cout<<x;
		}

	}





	return 0;

}